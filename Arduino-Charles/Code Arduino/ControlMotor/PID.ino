//void PIDsetup()
//{
//  vitesse_PID = Vitesse;
//  PID myPID(&vitesse_mesure_roue1, &vitesse_PWM1, &vitesse_PID,76.3,0.9457,0, DIRECT);
////  //initialize the variables we're linked to
////  Setpoint = 100;
////  //tell the PID to range between 0 and the full window size
////  PID_roue1.SetOutputLimits(0, 255);
////  PID_roue2.SetOutputLimits(0, 255);
////  PID_roue3.SetOutputLimits(0, 255);
////  PID_roue4.SetOutputLimits(0, 255);
////  //turn the PID on
////  myPID.SetMode(AUTOMATIC);
////  
//  
//
//}
//
//void PIDrun()
//{
//  
//
//}
